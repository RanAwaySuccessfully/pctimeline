# PCTimeline

Run it locally using `./server.sh` (uses Deno) or with any other static file HTTP server of your choice.

The `PCTimeline.xlsx` file serves as an example spreadsheet, and you can edit it however you like. Do not remove the header/first row (which contains the column names), but feel free to add or remove as many other rows as you want.